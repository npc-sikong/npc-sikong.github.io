# 哈希测试运营后台与 H5 演示原型协作规范

## 项目身份与边界

- 本项目唯一工作目录是 `/Users/sikon/Projects/haha`，项目名为 `hash-admin-demo`。
- 这是由哈希测试运营后台和用户端 H5 组成的纯前端演示原型，不开发 API、服务端、数据库、真实鉴权、真实支付或真实链上能力。
- 除非用户明确要求，不从其他原型、旧备份、发布目录或仓库复制、同步业务源码与构建产物。
- 未明确要求时，只做本地修改、运行和验证；不提交 Git、不推送、不发布。
- 用户已有改动均应保留。修改前先检查相关文件，不覆盖无关内容。

## 固定产品要求

- 对照后台为 `https://stg-admin.hash001.top`，对照 H5 为 `https://stg.hash001.top`。目标站、用户截图及用户最新反馈是页面结构和交互的主要依据。
- 现有 1:1 复刻阶段已经完成，当前代码冻结为“基础复刻基线”。从本条记录之后开始，用户提出的内容默认属于基线之上的新需求，不再视为补复刻或查漏阶段。
- 新需求的业务规则优先于目标站现状。不得因为目标站没有对应功能或页面存在差异，就删除、回退或弱化已经确认的新需求。
- 目标站后续主要用于沿用既有视觉、组件、字段命名和操作习惯；只有用户明确要求重新核对，或现有交互依据确实不足时，才重新进入逐页复刻审计。
- 运营后台始终使用白色浅色主题；所有后台页面、弹窗、表格、下拉层和空状态都不得切回黑底或暗色主题。用户端 H5 按 H5 目标站视觉复刻。
- 运营后台不保留登录页和首页。访问 `/`、`/login` 或 `/workbench` 时应直接进入 `/member/list`；侧栏和页签栏不得恢复“工作台”入口。用户端 H5 的登录、注册和首页属于复刻范围，统一使用 `/front/*` 路由，不占用后台入口。
- 页面语言为简体中文，保持目标运营后台的高密度表格、筛选区、按钮层级、弹窗尺寸和信息层级。
- 所有业务数据均使用确定性的前端模拟数据；刷新页面后允许恢复初始演示数据。
- 当前以下三个路由按目标站现状保留为空白页，除非用户提供新依据：
  - `/setting/website/withdraw-account`
  - `/setting/website/information`
  - `/setting/website/protocol`
- 当前运营后台覆盖 79 个非首页路由：76 个基础复刻路由、“版本说明”基础入口，以及新增的风控页面和“团队分析表”；用户端 H5 另覆盖目标站公开构建中的 66 个 `/front/pages/*` 路由。新增或修改页面时不得破坏其他路由。
- 用户端必须按标准 H5 实现：以 375px 为设计基准，适配常见 320–430px 手机宽度，正确处理顶部/底部安全区、固定导航、独立滚动容器、底部弹层和触控热区；桌面预览只显示居中的 H5 画布，不拉伸成桌面站。
- 用户端同样只使用确定性的前端模拟数据。登录、充值、提现、投注、红包、开户链接、验证码和区块验证均只产生本地状态或演示反馈，不调用真实业务接口，不发起真实资金、账号或链上操作。

## 代码结构约定

- `src/App.jsx`：应用外壳、通用组件、页面交互和特殊页面组件。
- `src/data.js`：侧栏路由、页面表格配置、模拟数据、`modalSchemas` 和 `detailSchemas`。
- `src/styles.css`：全局浅色主题和组件样式。
- `src/requirements.js`：后续新需求的唯一登记数据，以及版本、模块和菜单标记的派生规则。
- `src/RequirementFeatures.jsx`：版本说明页面、页面说明入口和业务说明弹窗。
- `src/requirements.css`：版本说明与业务说明功能的浅色主题样式。
- `src/TeamAnalysisPage.jsx`、`src/team-analysis.css`：代理管理“团队分析表”的筛选、分币种报表、导出与专用浅色样式。
- `src/GameRiskControlPage.jsx`：风控页面及其前端演示状态与交互。
- `src/risk-control.css`：风控页面和导航预警角标样式。
- `src/main.jsx`：React/Vite 入口。
- `src/storefront/`：用户端 H5 的独立应用壳、66 路由清单、页面域组件、模拟数据、弹层与命名空间样式。
- `public/storefront/`：从目标 H5 核对并本地化的演示图片资源。
- 优先复用现有 `DataPage`、`DataTable`、`FormDialog`、`DetailDialog`、`ConfirmDialog`、`Pagination` 和 Toast 机制，不为相同模式复制新组件。
- 新增或补全表单操作时，同步维护 `modalSchemas`；新增或补全详情/查看操作时，同步维护 `detailSchemas`，并确保操作名称能被 `handleAction` 正确分派。
- 详情弹窗中的不同页签必须展示各自对应的字段、表格或空状态，不得用同一组通用内容重复填充所有页签。
- 新建的 JS/JSX/数据文件原则上不超过 1200 行。若后续较大改动会继续放大 `App.jsx` 或现有超长样式文件，应按页面域或组件拆分；不要为了行数限制进行与需求无关的全面重构。
- H5 不复用后台的 `.panel`、`.btn`、`.modal-*` 等全局类名，统一使用 `sf-` 命名空间或等价隔离方式，避免前后台样式互相污染。H5 需要二级、三级弹层时使用可堆叠的独立弹层状态，不受后台单弹窗状态限制。

## 交互验收标准

- 页面上出现的每个可见按钮都必须有可观察反馈，不能保留无反应的占位按钮。
- 查询、重置、刷新、同步：展示加载状态或明确 Toast，并更新可见状态。
- 新增、编辑、导入、设置：打开业务对应表单，提供必填校验、取消、确认和成功反馈；适合时同步更新本地表格数据。
- 详情、查看、数据来源、盘口配置、投注记录、权限设置：打开对应详情弹窗，所有页签均可切换且内容不为空；无记录时显示“暂无数据”。彩票彩种配置的“玩法赔率”进入独立的彩票玩法赔率配置页，行内“编辑”再打开专用编辑玩法弹窗。
- 删除、清退、归集、审核、驳回、终止、重置区块、重新派生、手动结算等高风险操作：先展示二次确认，再展示成功或取消反馈。
- 复制、导出、下载、分页、每页条数、开关和批量操作必须产生真实的前端演示反馈。
- 弹窗页签栏必须保持固定可点击高度，并支持横向滚动；窄窗口下不得被弹窗内容压缩或遮挡。
- 所有模拟操作只修改浏览器本地状态，不向目标站或任何真实服务写入数据。
- 用户端 H5 的每个可见入口同样必须有可观察反馈；会产生真实资金、账号、验证码或链上副作用的目标功能只复刻流程和结果状态，绝不连接真实能力。

## 目标站核对规则

- 用户要求逐页复刻或重新核对时，必须使用 `browser:control-in-app-browser` 检查目标站和本地页面。
- 核对顺序以 `src/data.js` 的路由清单为准，跳过首页；至少检查页面标题、筛选项、工具栏、表头、行操作、弹窗、页签、确认框、空状态和操作反馈。
- 优先通过可见 DOM、截图和实际点击确认行为，不只依赖源码推测。
- 登录凭据、验证码或其他敏感数据不得写入源码、文档、日志或模拟数据。需要在浏览器输入密码或完成人机验证时，遵守浏览器工具的逐次确认要求。
- 目标站暂时不可访问时，可以先依据已保存的截图和既有审计结果继续本地工作，但交付时要明确说明尚未完成的在线核对范围。

## 版本说明与页面业务说明

- “版本说明”和每个新增或修改模块的“业务及需求说明”是本原型下一阶段的必备基础能力。现有逐页复刻成果是后续新需求的业务基线。
- “用户端 H5”切换入口固定展示在运营后台主头部的顶部居中区域，不占用左侧菜单；用户端 H5 同样在画布顶部居中提供“返回运营后台”，两端只做本地原型路由双向切换。“版本说明”仍位于后台左侧“会员管理”上方且不依附工作台。
- “版本说明”按版本和业务模块展示；每项新增或修改内容必须包含完成时间、模块说明、修改说明、实际修改点、验收说明和可点击的页面跳转。
- 当前已实现内容先记录为“基础复刻”基线。在用户明确指定首个版本号前不擅自编号；后续只有收到用户明确的版本升级指令时才新增或切换版本，不按日期或自然周自动升级。
- 用户尚未指定首个版本号时，后续需求统一归入不带编号的“待定版本”分组；收到明确版本号后再整体改名，不得因为缺少版本号而漏记任何修改。
- 从基础复刻完成后的第一项业务改动开始，全部按“新需求”登记。每项说明必须明确相对基础复刻新增或修改了什么，不能把新需求混写成原后台已有能力。
- 同一模块在同一版本中反复调整时，用最新完整说明覆盖该模块原说明，不堆叠微调过程或重复历史。
- 新增模块的左侧导航项右侧显示文字标记“(新)”，修改既有模块时显示“(改)”；括号是标记的一部分，不得省略。一级菜单因下级变化而受影响时显示“(改)”，只有一级菜单本身为全新模块时才显示“(新)”。标记以该模块最近一次需求记录为准。
- 每个新增或修改的非空业务页面，必须在页面内容左上方提供完整文字入口“业务及需求说明”，同时展示“(新)”或“(改)”及最近修改时间；点击后以弹窗展开详细说明。不得只用图标、悬浮提示或难以辨识的“说明”代替。
- 用户端 H5 的现有页面属于目标站基础复刻，不加入“版本说明”、页面需求说明或“(新)/(改)”标记。只有用户以后明确提出超出目标站复刻基线的新业务需求时，才为实际变化的 H5 页面登记说明；运营后台业务页仍执行上一条规则。
- 页面完整说明至少包含：页面功能说明、主要字段说明、查询/查看逻辑、操作逻辑、状态逻辑、金额或次数逻辑、关联模块、本次业务需求、功能验收说明和修改记录。
- 修改记录每条包含修改时间、修改说明和修改内容。修改说明讲清业务目的；修改内容列出页面、筛选、字段、弹窗、页签、规则、反馈或跳转等实际变化，不写开发过程流水账。
- 基础阶段结束后的每一次修改，无论用户是否再次提醒，都必须在同一次任务中自动生成或更新对应的需求说明与修改记录。页面代码改完但记录未同步，视为任务未完成。
- 每次新增或修改页面、模块、字段、弹窗、页签、规则或操作反馈时，必须同步维护唯一的需求登记数据，并由该数据自动生成：
  1. 当前版本“版本说明”中的模块记录、实际修改点、验收说明和页面跳转；
  2. 对应导航项右侧的“(新)”或“(改)”标记；
  3. 对应页面左上方的“业务及需求说明”入口、最近修改时间、详细弹窗和修改历史。
- 不得分别手写三套互不关联的说明。版本页、菜单标记和页面弹窗必须共用 `src/requirements.js` 中的同一份需求登记数据，确保新增一条记录后各处自动同步。
- 本次新增“版本说明”、导航标记生成机制和页面说明弹窗机制仍属于基础能力建设，明确豁免：不为本次修改新增版本记录、页面修改说明或“(新)/(改)”标记。从用户下一项业务需求开始执行自动记录规则。
- 用户明确将某次任务指定为“重新复刻”、基础对齐或复刻遗漏修正时，该任务属于基础基线纠偏，不生成版本记录、页面需求说明或“(新)/(改)”标记。此豁免只适用于用户明确指定的当次范围，不得扩展到后续业务需求。
- 涉及金额、比例、次数或汇总的数据逻辑时，说明中必须给出产品和运营可以直接理解的计算公式、边界与示例。
- 说明内容面向产品、运营和验收人员，不出现源码路径、组件名、接口、数据库或其他技术实现细节，也不把尚未实现的能力写成已完成。
- 上述说明均使用前端静态演示数据，不提供页面内编辑、保存或后端同步能力。
- 三个约定空白页在仍为空白时不强行添加说明；收到对应新需求并开始实现后，再同时补齐版本记录和页面业务说明。

## 开发与验证流程

1. 开始前确认当前目录为 `/Users/sikon/Projects/haha`，并阅读本文件及相关源码。
2. 先检查现有实现和用户改动，再做最小范围修改。
3. 能自行运行时，直接启动或复用本地 Vite 服务，不把启动步骤转交给用户。
4. 默认后台预览地址为 `http://127.0.0.1:4173/member/list`，用户端 H5 预览地址为 `http://127.0.0.1:4173/front/pages/index/index`。如果端口已被本项目占用，应复用现有进程。
5. 修改后使用应用内浏览器实际打开相关页面，逐个点击本次涉及的弹窗、页签、确认和反馈。
6. 对全局性改动，巡检全部非首页路由，确认：
   - 路由可访问且标题正确；
   - 页面为白色浅色主题；
   - 没有登录页或工作台入口；
   - 没有 React 运行时错误；
   - 三个约定空白页之外不存在意外空白页面。
   - 66 个 H5 路由均可访问，在 320px、375px 和 430px 宽度下没有横向溢出，核心二级/三级弹层可达，且没有任何真实外部写操作。
7. 完成前运行 `npm run build`。构建失败或浏览器回归失败时不得宣称完成。
8. 交付时让本地预览停留在 `/member/list` 或用户正在核对的页面，并简要说明修改内容和验证结果。

## 需求记录原则

- `AGENTS.md` 只记录长期有效的项目边界、协作规范、开发流程和需求记录机制；具体页面字段、业务公式、金额示例、状态口径与验收内容只维护在当前版本的“版本需求说明”和对应页面的“业务及需求说明”中，不在本文件重复登记。
- 用户明确给出的、会长期影响本原型开发边界、协作方式或交付流程的决定，应更新到本文件；具体业务决定写入版本说明和对应页面说明，临时调试信息与一次性数据不写入。
- 后续每项业务需求和每次修改都必须自动纳入“版本说明”和对应页面的“业务及需求说明”，并同步生成菜单标记、修改记录和验收口径；不能只完成页面代码。
- 附件中其他总控、站点、代理或 H5 后台的具体业务规则仍不得直接搬入本项目；只复用其版本记录与页面说明机制。
- 需求冲突时，以用户在当前项目中的最新明确指令为准，并同步修正本文件中过时的项目约束。
